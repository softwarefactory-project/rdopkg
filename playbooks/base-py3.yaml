---
- hosts: all
  tasks:
    - name: Install dependencies for python3
      package:
        name: "{{ item }}"
        state: present
      with_items:
      - python3
      - python3-rpm
      - python3-tox
      become: true
